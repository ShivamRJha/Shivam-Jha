import Image from 'next/image';import Link from 'next/link';import { awards, education, experience, profile, projects, publications, researchInterests, skills } from '@/lib/content';
const Section=({id,title,children}:{id:string,title:string,children:React.ReactNode})=><section id={id} className="reveal"><div className="container"><div className="section-head"><div><p className="eyebrow">{id}</p><h2>{title}</h2></div></div>{children}</div></section>;
export default function Home(){return <main id="home"><div className="container hero"><div className="reveal"><p className="eyebrow">Researcher · Scientist · Engineer</p><h1>{profile.name}</h1><p className="tag">{profile.tagline}</p><p>{profile.intro}</p><div className="actions"><a className="btn primary" href="#research">View Research</a><a className="btn" href="#projects">View Projects</a><a className="btn" href={profile.cv}>Download CV</a><a className="btn" href="#contact">Contact Me</a></div><div className="social"><a href={`mailto:${profile.email}`}>Email</a><a href={profile.links.github}>GitHub placeholder</a><a href={profile.links.linkedin}>LinkedIn placeholder</a><a href={profile.links.googleScholar}>Scholar placeholder</a><a href={profile.links.orcid}>ORCID placeholder</a></div></div><div className="card viz" aria-label="Subtle scientific visualization"><span className="node n1"/><span className="node n2"/><span className="node n3"/></div></div><Section id="research" title="Research Interests"><div className="grid">{researchInterests.map(r=><article className="card" key={r.title}><h3>{r.title}</h3><p className="muted">{r.text}</p></article>)}</div></Section><Section id="projects" title="Research & Technical Projects"><div className="grid">{projects.map(p=><Link className="card" href={`/projects/${p.slug}`} key={p.slug}><Image className="thumb" src={p.thumbnail||'/media/project-placeholder.svg'} alt="Project thumbnail" width={600} height={380}/><p className="eyebrow">{p.category} · {p.status}</p><h3>{p.title}</h3><p className="muted">{p.shortDescription}</p>{p.technologies.map(t=><span className="pill" key={t}>{t}</span>)}</Link>)}</div></Section><Section id="publications" title="Publications"><div>{publications.map(p=><article className="paper" key={p.title}><p className="eyebrow">{p.status}</p><h3>{p.title}</h3><p>{p.authors}</p><p className="muted">{p.venue}</p></article>)}</div></Section><Section id="about" title="About"><div className="two"><div className="card"><p>{profile.about}</p></div><div className="card"><h3>Technical Skills</h3>{skills.map(s=><p key={s.group}><strong>{s.group}:</strong> {s.items.join(', ')}</p>)}</div></div></Section><Section id="experience" title="Experience"><div>{experience.map(e=><article className="timeline" key={e.organization+e.position}><p className="eyebrow">{e.date} · {e.location}</p><h3>{e.position}</h3><p><strong>{e.organization}</strong></p><ul>{e.highlights.map(h=><li key={h}>{h}</li>)}</ul></article>)}</div></Section><Section id="education" title="Education"><div>{education.map(e=><article className="timeline" key={e.degree}><p className="eyebrow">{e.date} · {e.location}</p><h3>{e.degree}</h3><p><strong>{e.institution}</strong></p><p>{e.grade}</p>{e.details.map(d=><p className="muted" key={d}>{d}</p>)}</article>)}</div></Section><Section id="cv" title="Curriculum Vitae"><div className="card"><p className="muted">Replace the placeholder PDF through the admin CV workflow or by uploading a final CV to public/cv.</p><a className="btn primary" href={profile.cv}>Download CV (PDF)</a></div></Section><Section id="contact" title="Contact"><div className="two"><div className="card"><p>Email: <a href={`mailto:${profile.email}`}>{profile.email}</a></p><p className="muted">Phone is kept in structured content but not emphasized publicly.</p><p>References available upon request.</p></div><form className="card form"><input aria-label="Name" placeholder="Your name"/><input aria-label="Email" placeholder="Your email"/><textarea aria-label="Message" placeholder="Message for research collaboration or PhD discussion"/><button className="primary" type="button">Prepare email draft</button></form></div></Section><Section id="awards" title="Leadership, Workshops & Achievements"><div className="grid">{awards.map(a=><div className="card" key={a}>{a}</div>)}</div></Section></main>}
