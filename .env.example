# Static GitHub Pages version does not require environment variables.
# Keep this file as a reminder not to commit secrets.
# If a future authenticated CMS is added, document required variables here.
